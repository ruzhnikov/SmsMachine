general:
    path: '/opt/smsmachine'

logger:
    dir: 'log'
    file: 'smsmachine.log'

database:
    dbtype: sqlite # now avail only sqlite
    db_params:
        dbdir: 'data/db'
        dbfile: smsmachine.db

redis:
    host: localhost
    port:
    database: 3
